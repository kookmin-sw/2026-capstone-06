## todo-003-20260508

- [x] Landing.tsx 파일의 오류를 수정하고 프로젝트의 루트 경로("/")에 연결한다.
    - [x] `framer-motion` 라이브러리 설치
    - [x] `Landing.tsx`의 잘못된 import(`motion/react`)를 `framer-motion`으로 수정
    - [x] `Landing.tsx` 내의 링크들을 `/auth` 및 `/user` 경로로 업데이트
    - [x] `routes.tsx`에서 루트 경로("/")를 `Landing` 컴포넌트로 변경
