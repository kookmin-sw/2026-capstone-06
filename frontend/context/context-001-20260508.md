# context-001-20260508

## 작업 요약
- `Landing.tsx` 페이지를 프로젝트의 첫 화면으로 설정하고 관련 오류를 수정하였습니다.

## 상세 변경 사항
### 1. 의존성 추가
- 애니메이션 효과를 위해 `framer-motion` 라이브러리를 설치하였습니다.

### 2. `src/pages/Landing.tsx` 수정
- **Import 수정**: 존재하지 않는 `motion/react` 대신 `framer-motion`을 사용하도록 변경하였습니다.
- **Type 에러 방지**: `React.ReactNode` 사용을 위해 `React` import를 추가하였습니다.
- **경로 연결**: "무료로 시작하기", "로그인", "회원가입" 버튼이 `/auth` 페이지로 이동하도록 `Link` 컴포넌트를 적용하였습니다.

### 3. `src/routes.tsx` 수정
- 기존에 `/` 경로에서 `/auth`로 바로 리다이렉트되던 설정을 `Landing` 페이지를 보여주도록 변경하였습니다.

## 특이사항
- `react-router` v7을 사용 중이므로 `Link` 컴포넌트는 `react-router`에서 가져와 사용하였습니다.
- `framer-motion` 설치 시 PowerShell 실행 정책 이슈로 인해 `cmd /c`를 통해 설치를 진행하였습니다.

## 다음 작업 제안
- `Landing` 페이지에서 `로그인` 버튼 클릭 시 `/auth`로 이동한 후, 실제 로그인 로직 연동 확인 필요.
- 모바일 환경에서의 `Landing` 페이지 레이아웃 점검.
