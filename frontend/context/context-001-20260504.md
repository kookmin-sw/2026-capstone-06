# 작업 내역 요약 (2026-05-04)

## 완료된 작업
- `todo/todo-002-20260504.md`에 명시된 요구사항에 따라 페이지 구조 분리 및 통합 작업 수행.
- `src/pages` 내부에 `admin`, `user`, `auth` 디렉토리 생성.
- 기존 메인 앱의 페이지 컴포넌트(`Dashboard.tsx`, `Statistics.tsx`, `Settings.tsx` 등)를 `src/pages/user/` 로 이동.
- `adminAndLogin` 프로젝트 내의 관리자 페이지 컴포넌트(`AdminDashboard.tsx` 등)를 `src/pages/admin/` 으로 복사하여 통합.
- 로그인/회원가입 관련 `AuthPage.tsx` 컴포넌트를 `src/pages/auth/` 로 복사하여 통합.
- 새로운 디렉토리 구조에 맞춰 `routes.tsx`의 라우팅 설정 변경 (`/user/*`, `/admin`, `/auth`).
- `src/layouts/Sidebar.tsx` 내부의 링크를 `/user/*` 경로로 업데이트.
- 각 페이지 컴포넌트 내부의 상대 경로 임포트(`import ... from "../components/ui/*"`)를 새로운 디렉토리 뎁스(`"../../components/ui/*"`)에 맞게 일괄 수정.
- 파일 이동 중 발생한 인코딩 문제 해결을 위해 일괄 UTF-8 변환 적용.

## 특이사항
- `/` 경로 접근 시 `/auth` 로 리다이렉트 되도록 초기 설정 추가.
- 관리자 및 유저 측 UI 컴포넌트는 `src/components/ui/` 경로에 공통된 shadcn/ui 컴포넌트 폴더를 공유하여 사용.
- `adminAndLogin` 폴더는 더 이상 빌드되지 않으며 통합이 완료되었으나, 백업 목적으로 현재 보존 중 (추후 삭제 권장).
