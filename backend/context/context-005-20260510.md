# context-005-20260510: Git 추적 제외 관리 (read_code_api.py 등)

## Status
- [x] Completed

## Context
`.gitignore`에 등록되어 있었으나 이미 Git에 의해 추적(Track)되고 있던 임시/데이터 파일들을 추적 대상에서 제외함.

## Decision
- `git rm --cached` 명령어를 사용하여 다음 파일들을 Git 인덱스에서 제거:
  - `read_code_api.py`
  - `read_excel.py`
  - `dashboard_api.txt`
  - `code_api.txt`

## Consequences
- 로컬 파일 시스템에는 해당 파일들이 그대로 유지됨.
- 향후 Git 커밋 시 해당 파일들의 변경 사항이 무시되며, 원격 저장소로 더 이상 올라가지 않음.
