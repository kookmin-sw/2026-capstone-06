# todo-007-20260505.md

## List
- [ ] 문자열 기반의 PK/FK관계는 성능상 문제가 발생할 수 있으므로, 이를 숫자 기반의 PK/FK로 변환
    - [ ] Code.java에서 Long seq를 PK로 추가하고 String code는 유니크 인덱스가 걸린 일반 컬럼으로 변경
    - [ ] Device.java에서 User와 join을 할 때, memberId로 조인하지 말고, User의 Long seq로 join하도록 변경
    - [ ] Hospital.java에서 mainMedCode와 medCodes를 String이 아닌 Code 엔티티 참조로 변경
    - [ ] medCodes (리스트 참조) 해결. 현재 @ElementCollection으로 문자열 리스트를 별도 테이블에 저장하고 있는데, 이를 Code 엔티티와의 @ManyToMany 관계로 변경
    - [ ] PetHouse entity에서 Code엔티티의 code를 문자열로 참조하지 말고, Code의 Long seq로 참조하도록 변경
- [ ] 엔티티의 PK/FK를 숫자 기반으로 변경하였을 때, 이를 사용하는 코드들이 있다면 모두 수정